<form method="post" action="{{ path('app_patologiadetectada_delete', {'id': patologiadetectada.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ patologiadetectada.id) }}">
    <button class="btn">Delete</button>
</form>
