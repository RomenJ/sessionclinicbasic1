{% extends 'base.html.twig' %}

{% block title %}Patologiadetectada{% endblock %}

{% block body %}
    <h1>Patologiadetectada</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ patologiadetectada.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ patologiadetectada.name }}</td>
            </tr>
            <tr>
                <th>Deacription</th>
                <td>{{ patologiadetectada.deacription }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_patologiadetectada_index') }}">back to list</a>

    <a href="{{ path('app_patologiadetectada_edit', {'id': patologiadetectada.id}) }}">edit</a>

    {{ include('patologiadetectada/_delete_form.html.twig') }}
{% endblock %}
